import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        flex: 1,
        backgroundColor: '#fff',
        justifyContent: 'center',
        alignItems: 'center',
    },
    timeText: {
        color: '#000',
        fontSize: 75,
    },
    dateText: {
        color: '#000',
        fontSize: 40,
    }
})